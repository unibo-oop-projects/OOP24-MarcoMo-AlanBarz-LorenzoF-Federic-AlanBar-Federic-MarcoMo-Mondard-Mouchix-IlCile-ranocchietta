package frogger.model;

import frogger.common.Position;

public class Car extends MovingObject {

    public Car(Position pos, int dimension, float speed, int direction) {
        super(pos, dimension, speed, direction);
    }

}
